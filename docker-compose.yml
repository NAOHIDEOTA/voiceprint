services:
  voiceprint:
    container_name: voiceprint
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/workspace
    ports:
      - "8081:8081"
    command: ["tail", "-f", "/dev/null"]
